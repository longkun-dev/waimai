spring:
  application:
    name: api-service
  profiles:
    active: dev
  cloud:
    config:
      fail-fast: true
      uri: http://localhost:8010
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
